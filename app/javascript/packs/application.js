import ReactOnRails from 'react-on-rails';

import Directory from '../bundles/Directory/components/Directory';

// This is how react_on_rails can see the Directory in the browser.
ReactOnRails.register({
  Directory
});
